html(lang="en")
  head 
    title List of users
  style
  body
    h1 Users list
    ul
      each user in users 
        li 
          <a href="user/#{user.id}">#{user.firstName} #{user.lastName}</a>
